server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhengaobinlonglive/3.27test.git
          search-paths:
          -configfiles
        label: dev